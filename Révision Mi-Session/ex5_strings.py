
# Q1
# Écrire une fonction q1_str_nieme(texte: str, n: int) -> str qui prend en paramètre un texte et un entier n et qui
# retourne le n-ième caractère du texte. Si n est plus grand que la longueur du texte, retourner une chaîne vide.
def q1_str_nieme(texte: str, n: int) -> str:
    return ""


# Q2
# Écrire une fonction q2_str_milieu(texte: str) -> str qui prend en paramètre un texte et qui retourne le caractère du
# milieu du texte. Si la longueur du texte est paire, retourner une chaîne vide.
def q2_str_milieu(texte: str) -> str:
        return ""


# Q3
# Écrire une fonction q3_str_split(texte: str) -> list[str] qui retourne une liste contenant les mots du texte qui sont
# séparés par le caractère de tabulation. Vous pouvez utiliser la méthode split() des str.
def q3_str_split(texte: str) -> list[str]:
    return []


# Q4
# Écrire une fonction q4_str_slice(texte: str) -> str qui prend en paramètre un texte et qui retourne la tranche de
# texte allant du milieu de la chaîne jusqu'à la fin. Si la longueur du texte est impaire, le milieu comprendra le
# caractère du milieu.
def q4_str_slice(texte: str) -> str:
    return ""


# Q5
# Écrire une fonction q5_str_replace(texte: str, ancien: str, nouveau: str) -> str qui prend en paramètre un texte, un
# ancien mot et un nouveau mot et qui retourne une nouvelle chaîne où toutes les occurrences de l'ancien mot sont
# remplacées par le nouveau mot.
def q5_str_replace(texte: str, ancien: str, nouveau: str) -> str:
    return ""


# Q6
# Écrire une fonction q6_str_replace_fin(texte: str, ancien: str, nouveau: str) -> str qui prend en paramètre un texte,
# un ancien mot et un nouveau mot et qui retourne une nouvelle chaîne où seulement les occurrences de l'ancien mot sont
# remplacées s'ils sont dans les 12 derniers caractères du texte.
def q6_str_replace_fin(texte: str, ancien: str, nouveau: str) -> str:
    return ""
