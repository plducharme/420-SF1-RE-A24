import random


# Q1
# Écrire une fonction q1_prix_zoo(age: int) -> float qui prend en paramètre l'âge d'un visiteur et qui retourne le prix
# d'entrée au zoo selon les critères suivants:
# - Gratuit pour les enfants de 0 à 2 ans
# - 5.50$ pour les enfants de 3 à 12 ans
# - 9.00$ pour les adultes de 13 à 64 ans
# - 6.50$ pour les aînés de 65 ans et plus
def q1_prix_zoo(age: int) -> float:
    return 0


# Q2
# Écrire une fonction q2_upper_lower(texte: str) -> str qui prend en paramètre un texte et qui retourne ce texte en
# majuscules si le texte commence par une majuscule et en minuscules sinon.
def q2_upper_lower(texte: str) -> str:
    return ""


# Q3
# Écrire une fonction q3_jeu_de_dés() -> str qui simule le lancer de deux dés et qui retourne True si les deux dés ont
# la même valeur et False sinon.
random.seed(42)


def q3_jeu_de_dés() -> bool:
    return False


