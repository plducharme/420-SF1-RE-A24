import random
import math

# Question 1
# Écrire une fonction q1_random(a: int, b: int) -> int qui prend en paramètre deux entiers a et b et qui retourne un
# entier aléatoire entre a et b inclusivement. Vous pouvez utiliser la fonction randint() du module random.
random.seed(666)


def q1_random(a: int, b: int) -> int:
    return 0


# Question 2
# Écrire une fonction q2_somme_de_deux_dés() -> int qui simule le lancer de deux dés et qui retourne la somme des deux
random.seed(42)


def q2_somme_de_deux_dés() -> int:
    return 0


# Question 3
# Écrire une fonction q3_roche_papier_ciseaux() qui va retourner un choix aléatoire parmi une liste de choix comprenant
# les éléments "roche", "papier" et "ciseaux".
random.seed(19)


def q3_roche_papier_ciseaux() -> str:
    return ""


# Question 4
# Écrire une fonction q4_sin_en_degrés(x: float) -> float qui prend en paramètre un angle en degrés x et qui retourne le
# sinus de cet angle. Vous pouvez utiliser la fonction sin() du module math. N'oubliez pas de convertir x en radians
def q4_sin_en_degrés(x: float) -> float:
    return 0


# Question 5
# Écrire une fonction q5_refraction(n1: float, n2: float, angle: float) -> float qui prend en paramètre les indices de
# réfraction n1 et n2 de deux milieux et un angle d'incidence angle et qui retourne l'angle de réfraction en degrés
# selon la loi de Snell-Descartes. Vous pouvez utiliser la fonction asin() du module math. N'oubliez pas de convertir
# les angles selon ce qui est requis.
def q5_refraction(n1: float, n2: float, angle: float) -> float:
    return 0


# Question 6
# Écrire une fonction qui prend deux str en paramètre qui représentent des nombres binaires et qui retourne une str de
# la somme de ces deux nombres en binaire. Vous pouvez utiliser la fonction int() pour convertir un nombre binaire en
# décimal et la fonction bin() pour convertir un nombre décimal en binaire.
def q6_addition_binaire(a: str, b: str) -> str:
    return ""

