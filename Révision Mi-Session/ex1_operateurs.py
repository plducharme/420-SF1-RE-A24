

# 1 Écrire une fonction q1_reste_division(a: int, b: int) -> int qui prend en paramètre deux entiers a et b et qui
# retourne le reste de la division de a par b. Par exemple, q1_reste_division(10, 3) retourne 1.
def q1_reste_division(a: int, b: int) -> int:
    return 0


# 2 Écrire une fonction q2_hypotenuse(a: float, b: float) -> float qui prend en paramètre les longueurs des deux côtés
# perpendiculaires d'un triangle rectangle et qui retourne la longueur de l'hypoténuse. Par exemple, q2_hypotenuse(3, 4)
# retourne 5.0.
def q2_hypotenuse(a: float, b: float) -> float:
    return 0


# 3 Écrire une fonction q3_priorite(a: int, b: int, c: int) -> int qui prend en paramètre trois entiers a, b et c et qui
# retourne le résultat de l'exponentiation de la somme de a et b par c. Par exemple, q3_priorite(2, 3, 4) retourne 625.
def q3_priorite(a: int, b: int, c: int) -> int:

    return 0


# 4 Écrire une fonction q4_moyenne(a: float, b: float, c: float) -> float qui prend en paramètre trois nombres à virgule
# flottante a, b et c et qui retourne la moyenne de ces trois nombres. Par exemple, q4_moyenne(2.5, 3.5, 4.5)
# retourne 3.5.
def q4_moyenne(a: float, b: float, c: float) -> float:
    return 0


