# Le discriminant d'un polynôme du second degré (ax^2 + bx + c = 0 où a != 0) est donné par la formule suivante:
# Δ = b² - 4ac
# Calculer et afficher le discriminant pour ces coefficients:
a = 3
b = -9
c = 8



# Résoudre l'équation quadratique en utilisant le discriminant.
# x = -b ± Δ**0.5 / 2a
# Si le discriminant est positif, il y a 2 solutions réelles.
# Si le discriminant est nul, il y a 1 solution réelle.
# Si le discriminant est négatif, il n'y a pas de solution réelle.
# Afficher les solutions réelles s'il y en a.
a2 = 1
b2 = 4
c = -10
